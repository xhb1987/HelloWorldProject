import RNC from 'react-native-css';

const styles = RNC`
    tabContainer: {
        flex-direction: row;
    }

    tab {
        flex: 1;
        height: 50px;
        background-color: black;
    }
    `;

export default styles;
