import RNC from 'react-native-css';

const styles = RNC`
    container {
        background-color: white;
        padding: 10px;
    }
    title {
        font-size: 18px;
        font-weight: bold;
        margin-bottom: 10px;
    }
    content {
        font-size: 15px;
        line-height: 20px;
    }
    `;

export default styles;
