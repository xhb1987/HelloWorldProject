import RNC from 'react-native-css';

const styles = RNC`
    container {
        flex-direction: row;
        height: 50px;
    }

    button: {
        text-align: center;
        color: black
    }
    `;

export default styles;
